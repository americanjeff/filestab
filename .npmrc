; Project-local pnpm store: the agent sandbox's global store dir
; (~/.local/share/pnpm/store) is on a read-only filesystem, so pin the store
; inside the repo (gitignored). On a normal machine this just uses a local
; store instead of the shared one — no behavioral difference for installs.
store-dir=.pnpm-store
